# progress — worktree-tree-colors

## 2026-08-04 — 给树冠增加金、黄、红等秋色，新增枫树与平展树

分支：`worktree-tree-colors`

### 需求

现有树冠颜色不错，但希望增加更多颜色：金色、黄色、红色的树，还有枫树，以及像小叶榄仁那样平展分层的树。

### 改动

**`src/textures.js`**

- 新增 `drawMapleLeaf()`：五裂掌状枫叶。每个裂片是同一 path 里独立的一去一回曲线对，所以裂口一直切到叶基 —— 在树冠距离上，这些裂口就是「枫叶」和「又一片椭圆叶」的全部区别。另加一条长叶柄，叶柄在叶片间露出来是树冠读起来像落叶树的主要原因。
- `makeCanopyTexture()` 增加 `leaf: 'maple'` 选项，切换叶形并调整簇密度。

**`src/trees.js`**

- 新增两条树冠轮廓曲线：
  - `tiers` —— 小叶榄仁的另一半特征。`umbrella` 给出的是一个平滑扁平体，读起来像伞；真实的树是**分层**的，层与层之间能看见天空。做法是用一个缓慢的径向波调制同一个宽轮廓包络，每个波谷把车削面掐进去，读作两层枝叶之间的空隙。
  - `spire` —— 窄高的火焰形（杨树）。低处就达到最大宽度并一直保持到接近顶端，这是它和 `cone` 的区别：针叶树是全程收窄，杨树是带圆肩的柱子。
- 新增四个暖色树种：红枫（`dome` + 掌状叶）、琥珀色枫（橙，`dome` + 掌状叶）、黄杨树（`spire`）、金色小叶榄仁（`tiers`）。
- 配套新增 `mapleBark`（暖红棕）、`poplarBark`（近桦木的浅灰绿）。
- 绿色树种数量下调（pagoda 70→58、pine 90→76、highPine 45→38、ginkgo 38→26），暖色树种不是叠加在原有森林之上，而是替换掉一部分。

### 三个通过实测发现并修掉的问题

1. **暖色压倒画面。** 四个暖色树种起初都放在近水距离带（照抄了 ginkgo 的写法），结果开场视角整个前景都是秋色，绿色被推到远坡 —— 点缀变成了主体。改为把枫树/琥珀/杨树推到中远距离带（minD 30/34/40），近景保持绿色。金色小叶榄仁保留近水位置，因为分层轮廓只在近处才读得出来，代价是数量压到 16。最终绿 288 : 暖 96（25%）。

2. **枫叶贴图太稀。** 掌状叶大部分面积是裂口，覆盖率远低于同长度的椭圆叶，据此把每簇叶片数降到椭圆叶的一半后，贴图出现可见斑点 —— 不是通透感而是背景透出来的麻点。用**叶片数**补回覆盖率（叶片放大只会在树冠距离上读成卷心菜）。实测 alpha 覆盖率：枫 85.8% vs 绿 85.4%，已对齐。

3. **`tiers` 只有一个腰。** 3.4 个周期只产生一个位于半高处的波谷、掐进 50% —— 是沙漏，不是分层树。改为 7 个周期后得到三个大致均匀的波谷（相对半径 0.75/0.64/0.42），读作四层；振幅必须随频率同步下调，否则会把树冠切成互相分离的圆盘。同时车削环数对该轮廓从 17 提到 40：17 个采样点撑不住 3.5 个周期，分层会走样成不规则抖动。

### 验证

- `npm run build` 通过（580.73 kB，仅有既有的 chunk 体积警告）。
- Chrome 实机渲染，控制台 0 error 0 warn。
- 五种颜色（绿/金/黄/橙/红）在默认开场视角与近景视角均正确渲染。
- 轮廓曲线端点实测 `tiers(0)=0`、`tiers(1)≈1.3e-7`、`spire(0)=0`，两端归零，车削壳体闭合无端盖接缝。
- 分层数在叠加表面噪声之后实测仍为 3 个波谷 / 41 环，未被噪声吞掉。
- 临时调试钩子（`window.__dbg`）已移除，`git diff src/main.js` 为空。

### 遗留问题

**近景树冠上的黑色麻点**：不是本次引入。实测三张贴图（含原有绿色）在 alphaTest 0.12 存活的纹素中，暗于 0.25 亮度的占比均为 **0%**，所以麻点是既有通透（pierce）孔洞透出暗背景所致，与新增颜色无关。未改动 —— 属于既有行为，不在本次范围内。

---

## 2026-08-04 — 场景从单块 300 扩展为 3×3 分块流式加载

分支：`worktree-tree-colors`

### 需求

场景由一块 300×300 扩展为 3×3 共九块（900×900）。中间块保持为网格中心、保持原有开场构图；小溪沿对角线从西北角 (-450, 384) 流到东南角 (450, -384)；按距离动态加载/卸载（而非全部常驻），一次只建一块；新块直接出现，不做淡入。

### 改动

**`src/grid.js`（新增）**

- 随机数按**块**播种，而不是全局一条序列。原来 `main.js` 用固定种子覆盖 `Math.random`，这依赖「启动时按固定顺序构建一次」；现在块的加载顺序取决于玩家往哪走，同一片地会在两次访问间长出不同的树。改为 `withChunkRng(cx, cz, salt, fn)`：块内容是坐标的纯函数，加载顺序、重载次数、行走方向都影响不到它 —— 这是卸载后重新加载能保持一致的前提。
- `salt` 按子系统区分，否则地面/草/树会走同一条数列，草会长在树的位置上。
- `disposeGroup()` 只释放 geometry：材质与贴图是跨块共享的单例，卸载时释放会导致下次任何块用到时重新编译 shader，是一次可见的卡顿。

**`src/streamPath.js`**

- 曲线由 ±150 延长到 ±450，并加入 z 向漂移形成对角流向。
- 用**弧长**参数化测出中间段真实的 `MID_T0/MID_T1`（此前误用分段序号），`SPAN ≈ 0.25`。
- 阶地与水潭沿曲线按 `REPS=[-2,-1,0,1,2]` 重复，让整条谷地保持同样的物理落差节奏（约每 40 单位一级）。每侧两次而非一次：中间段只占弧长 0.25、两侧各约 0.37，只重复一次会让最外侧河段完全没有阶地和水潭，成为一条等宽的直渠。
- 水潭宽度按重复次序做置换，避免外侧看起来是中间段的克隆。
- `streamAt()` 改为 24 单位分桶的空间哈希 + 环形外扩搜索；采样数 2400（原 8 倍），保持与原 300 单位场景相同的沿流采样密度。

**五个模块改为按块构建**（`createXxx(scene, cx, cz)` 返回可释放的 Group）

- `ground.js`：每块一张 300×300 平面，顶点用世界坐标，材质共享。
- `grass.js`：每块 60k 草簇、8×8 子格，返回 `{group, cells, update(camXZ)}`。
- `trees.js`：贴图改为惰性单例（canvas 绘制很贵），`placeSpecies` 相对块原点，手摆的树只在中间块。
- `foliage.js` / `stream.js`：岸边植物、石头、水花沿**整条**曲线走一遍，再用 `mine(x, z)` 只保留落在本块内的 —— 跳过外块的抽取会让本块内容依赖于之前建了哪些块。

**`src/chunkManager.js`（新增）**

- 一块的构建切成 `STEPS = [ground, trees, stream, grass, foliage]`，按每帧 8ms 预算跑，能塞几步就几步然后交还渲染。顺序按「缺了最显眼」排：地面是地形轮廓，缺了就是个洞；树承载场景性格；草最贵，放在已经像森林之后；灌木层只在近处可读，最后。
- 加载 1.35 块距、卸载 1.85 块距 —— 两个阈值必须不同，单一半径会让停在边界上的相机每几帧重建又销毁同一块。
- 相机跨过半块格子才 `reprioritise()`，按距离排序，走向哪块就先拉哪块。

**`src/world.js`**

- 阴影框改为**跟随相机**并按 60 单位吸附，尺寸约一块大小。原来是覆盖整个 300 场景的静态框；场地变成 900 后，静态框会把 1024² texel 摊到 900 单位上（约 0.9m/texel），每棵树的影子都糊成灰斑。吸附而非连续跟随：每帧滑动不到一个 texel 会让所有阴影边缘闪烁，而且吸附后大多数帧根本不需要重画阴影图。
- `shadow.camera.far` 提到 900：掠射的太阳把影子拉得和场地一样长，超出这个 slab 的物体会静默地不再投影。
- 天空穹顶半径 500→900 并跟随相机，否则相机能走出穹壁、天空翻面。

**`src/toon.js`**

- `done` 集合从每次调用内提到模块级。块是陆续流入的，每块落地都会再跑一次 `toonify`，而材质是共享单例；每次调用一个新集合会把已打过补丁的材质重新打一遍，而每次打补丁都改写 `onBeforeCompile` 并改变缓存键，three.js 会把整套 program 丢掉重编九次。

### 实测发现并修掉的问题

1. **`getPointAt` 返回 undefined 导致运行时崩溃**（`npm run build` 查不出来，只在浏览器里炸）。`DROPS` 故意包含 t 在 0..1 之外的重复副本（`levelAt` 会直接忽略它们），但阶地石台是真实几何、必须在曲线上采样，而 `getPointAt` 在区间外返回 `undefined`。修法：按 `d.t` 跳过越界的落差 —— 跳过条件只依赖 `d.t`，所以每块跳过的是同一批，块间的随机抽取仍然对齐；抖动后的 `tt` 再 clamp 一次。
2. **`DROP_LEN` 与 `pool.s` 多乘了 3**：`SPAN` 本身已经完成缩放。
3. **`MID_T0/MID_T1` 误用分段序号**而非弧长，导致中间段没落在原场景的位置上。
4. **手摆的 hero 石头缺 `midT()` 映射**，在九块里各出现一次。
5. **地面亮度抖动直接用 `Math.random`**，块与块之间出现明显接缝。

### 验证

- `npm run build` 通过（586.02 kB，仅既有的 chunk 体积警告）。
- Chrome 实机：修掉 `getPointAt` 崩溃后，控制台 **0 error 0 warn**（含跨块行走之后）。
- **确定性**（本次架构的核心性质）：中途插进去建**别的**块，再重建同一块，geometry 指纹逐字节一致（ground 与 trees 各测 3 / 2 块）；不同块的指纹互不相同 —— 说明坐标与 salt 的混合没有退化。
- **接缝**：沿全部块边界每 3 单位采样，`terrainHeight` 两侧最大差 1.7e-6（浮点噪声量级）。
- **水面**：全曲线 3000 点采样，逆坡段 0，总落差 48 单位；中间段起止水位 -0.4 / 11.6，与原 300 单位场景**完全一致**（开场构图未移动）。
- **曲线覆盖**：端点实测 (-450, 384) → (450, -384)，与需求一致；2000 点采样无一个非法样本。
- **帧率**：冲刺跨越至少一条块边界再转向，900 帧内中位 16.6ms、p95 18.5ms、最差 20.7ms —— 时间预算生效，没有建块造成的卡顿。
- 开场视角截图核对：小溪、阶梯瀑布、hero 石头位置不变，暖色树种混入远处树线。
- 清理：验证用的 `.shot/` 截图目录已删除。

### 遗留问题

- **场地是有限的 3×3，走到边缘会看到地形尽头**。这是既有行为（原来 300 单位场景同样如此），只是边界推远了。无雾 + 远裁剪面 1200 让边缘可见；要消除需要改成无限网格或加边界约束，不在本次范围内。
- **常驻块数实测 3~9，而非预估的 4~6**。站在中心时九块全部落在加载距离内 —— 对角块最近的角只有 212 单位，而场景无雾、远裁剪面 1200，卸载它会留下可见的空洞。所以按距离动态加载仍然生效（走到角上降到 3 块），但在这个 3×3 的尺度上中心位置就是全驻留。要真正限制常驻数需要缩小加载半径并配合雾或更近的裁剪面，属于取景权衡，未改动。
- 近景树冠黑色麻点：同上一条记录，既有行为，未改动。
