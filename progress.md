# 性能优化记录

目标：降低 GPU 负载 / 发热。测试机 Apple M4，Chrome（ANGLE Metal），视口 1470×774，dpr 2，显示器 60Hz。

## 诊断：热从哪来

原版稳定 60 FPS，但这掩盖了问题 —— 帧率被 vsync 锁住，看不出余量。真实信号是 GPU 每帧耗时**中位 15.10ms**，而 60Hz 的预算是 16.67ms。GPU 几乎满载在维持 60 帧，所以持续高功耗、发烫。

按渲染目标拆解每帧的 236 万个三角形：

| 渲染目标 | 分辨率 | draw calls | 三角形/帧 | 占比 |
|---|---|---|---|---|
| 阴影贴图 | 1024×1024 | 26 | 861,330 | 37% |
| 主场景 | 2940×1548 (4.55 MPx) | 108 | 1,497,228 | 63% |
| bloom mip 链 | 1470×774 起 13 趟 | 13 | ~0 | 全屏片元开销 |

主场景那 149 万再按几何体拆开：

| 几何体 | 实例数 | 三角形/帧 |
|---|---|---|
| 树枝卡片 (20 tris/个) | 24,840 | 496,800 |
| 草丛 (18 tris/个) | 21,614 / 60,000 | 389,052 |
| 石头 (320 tris/个) | 557 | 178,240 |
| 灌木球 (180 tris/个) | 700 | 126,000 |
| 地面网格 | 1 | 96,800 |
| 树干 (252 tris/个) | 353 | 88,956 |

## 最重要的结论：这个场景是片元瓶颈，不是三角形瓶颈

这条决定了后面所有取舍，所以放在前面。

用 `EXT_disjoint_timer_query_webgl2` 反复测量后发现：**把每帧三角形从 149 万砍到 112 万，GPU 时间没有任何变化。** 最稳健的统计量 `gpuMin`（最小帧时间，受背景干扰最小）在五次测量、三种几何配置下全部落在 5.2~5.9ms，完全无差别。

瓶颈在片元侧：alpha 测试的树叶卡片大量重叠绘制（`alphaTest` 材质无法享受 early-Z），加上水面着色器每片元约 9 次 value noise。

**所以真正有效的杠杆是"少算像素"，而不是"少画三角形"。** 收益最大的两项改动（静态阴影、降低像素比）都属于前者。

## 已应用的改动

### 1. 阴影贴图只画一次（最大的一笔）

`src/main.js`

```js
renderer.shadowMap.autoUpdate = false;
renderer.shadowMap.needsUpdate = true;
```

太阳位置固定、几何体在 CPU 侧从不移动，唯一的运动是风的顶点着色器。而风的摆幅（0.08–0.1 世界单位）远小于一个阴影贴图纹素 —— 1024 的贴图铺在 360m 场景上，1 纹素 ≈ 0.35m。原本每帧重画的这 86 万个三角形，产出的阴影贴图逐帧几乎完全一样。

**省掉每帧 861,330 个三角形和 26 个 draw call，以及对应的 1024×1024 光栅化。视觉零变化。**

### 2. 像素比上限 2 → 1.5

```js
const MAX_PIXEL_RATIO = 1.5;
```

dpr 2 意味着着色的片元数是 dpr 1 的 4 倍。既然瓶颈在片元侧，这是单项收益最大的改动。这也是唯一一项**有意的画质权衡**：画面略微变软，把这个常量改回 `2` 即可还原。

**渲染像素 4.55 MPx → 2.56 MPx，片元工作量 −44%。**

### 3. bloom 降到半分辨率

`src/postprocess.js`。bloom 本就是一团宽泛柔光，模糊本身掩盖分辨率差异，而它由若干趟全屏 pass 组成，是后处理的主要成本。

顺手修掉一个既有的不一致：`EffectComposer.setSize()` 传给 pass 的是**设备像素**尺寸，而构造时传的是 CSS 像素，所以原版一旦触发窗口 resize，bloom 链会从 1470×774 悄悄跳到 2940×1548（成本×4）。加了 `setSize` 覆盖锁住比例：

```js
const bloomSetSize = bloom.setSize.bind(bloom);
bloom.setSize = (w, h) => bloomSetSize(Math.round(w * BLOOM_SCALE), Math.round(h * BLOOM_SCALE));
```

**bloom 首级从 1470×774 降到 552×291（片元数 ≈ 1/7），且 resize 后不再反弹。**

### 4. 关掉无效的 MSAA

`antialias: true` → `false`。

`EffectComposer` 自建 render target 且不设 `samples`（已核对 three r169 源码：`new WebGLRenderTarget(w, h, { type: HalfFloatType })`），场景画进这个 RT，所以默认帧缓冲的 MSAA 从未作用于任何场景几何 —— 它唯一能抗锯齿的对象是 `OutputPass` 那个全屏四边形，而全屏矩形没有内部边缘。等于白白分配一份全尺寸多重采样缓冲。

**省下一份 MSAA 缓冲的内存和带宽。视觉零变化。**

### 5. `preserveDrawingBuffer` 改成按需开启

这个标志只为截 README 用图（`canvas.toDataURL()`），代价是浏览器每帧都不能丢弃后台缓冲。改成查询参数触发：

```js
const CAPTURE = new URLSearchParams(location.search).has('capture');
```

截图时访问 `localhost:5173/?capture`。**视觉零变化。**

### 6. 摄像头之外的物体不再被处理（网格分块）

新增 `src/chunks.js`，被 `trees.js`（树枝卡片）和 `foliage.js`（灌木球）使用。

问题在于：单个 InstancedMesh 铺满整个 290m 场地时，它的包围球覆盖全场，**永远不可能被视锥剔除** —— 背对森林时，那 24,840 个树枝卡片和 700 个灌木球依然逐帧提交，连风的顶点着色器都在为看不见的它们运行。按网格把实例分桶、每格一个 mesh（共享同一材质，不增加着色器程序），three.js 就能整格丢弃。

`InstancedMesh.computeBoundingSphere()` 是逐实例 union 的，所以包围球精确；剔除的正确性已验证 —— 关掉剔除与开着剔除的画面只差 0.05%（等于噪声底），说明没有剔掉任何可见内容。

`GRID` 是真实的权衡而非免费的胜利，实测（约 1000 帧中位数）：

| 配置 | draw calls | 三角形/帧 | CPU/帧 |
|---|---|---|---|
| 不分块 | 121 | 1,497,240 | 2.4 ms |
| **GRID 3（当前）** | **147** | **1,295,849** | **2.6 ms** |
| GRID 6 | 187 | 1,116,972 | 3.1 ms |

GPU 时间三者无差别（见上文"片元瓶颈"）。GRID 6 虽然三角形最少，但多出的 66 个 draw call 让 CPU 涨了 0.7ms —— 那是可测量的倒退。**GRID 3 是既拿到剔除、又不付 CPU 代价的档位。** 若将来树木数量大增到真的变成顶点瓶颈，再调高。

## 结果

| 指标 | 原版 | 优化后 | 变化 |
|---|---|---|---|
| GPU 每帧（最小值） | 9.53 ms | 5.23 ms | **−45%** |
| GPU 每帧（p10） | 11.93 ms | 7.88 ms | **−34%** |
| GPU 每帧（p25） | 13.67 ms | 10.31 ms | **−25%** |
| 三角形/帧 | 2,358,570 | 1,295,849 | **−45%** |
| 渲染像素 | 4.55 MPx | 2.56 MPx | **−44%** |
| CPU/帧 | — | 2.6 ms | 预算充裕 |

关键在于从"贴着 16.67ms 的 vsync 天花板满载运行"变成"有明显余量"，GPU 得以降频，这才是退烧的原因。

**GPU 计时的可信度说明（重要）：** ANGLE-Metal 上 `TIME_ELAPSED_EXT` 较粗且包含呈现等待，加之测量期间浏览器其他标签页在活动，噪声很大 —— 同一份代码两次测量曾得到 median 11.03 和 16.02（±40%）。因此：

- **可信的是确定性计数器**：三角形数、draw call 数、渲染像素、CPU 时间（1000+ 样本的中位数）。
- **GPU 时间只用最小值/低分位做趋势判断**，不用中位数做精细比较。上表 GPU 行取自噪声较低的那批采样。
- 3~6% 级别的差异这套测量分辨不了，所以凡是落在这个量级的改动，我都按"无收益"处理。

## 视觉验证

比较渲染结果时踩了两个坑，都会伪造出巨大的像素差异：

1. **分辨率不同**：优化版画在 2205×1161，而截图按视口输出 2940×1548，浏览器会放大整张图，每个高频树叶像素都必然移位。
2. **动画相位不同**：风让树叶卡片持续摆动，树冠是 alpha 贴图的高频内容，几个像素的位移就能造成 70% 的像素差异。

正确做法：注入 `performance.now = () => 1000` **冻结时钟**（风、水流、飞鸟、浮尘全部定格），并把 dpr 临时设回 2 以匹配分辨率。

在此条件下，第 1–5 项改动与原版对比：

```
mean abs diff: 0.51 / 255
pixels >30: 0.05%    >80: 0.01%    >140: 0.00%
```

同一份代码两次加载的噪声底也是 0.05%，所以这五项在同分辨率下与原版**逐像素等价**。dpr 1.5 的柔化是叠加在此之上、可一行还原的主动选择。

（PNG 解码 + 差异统计脚本无第三方依赖，用 Node 的 zlib 手写；仓库内未留下临时图片。）

## 已知副作用：森林布局重洗了一次

第 6 项分块**改变了场景布局**（树木、花草、石头的位置全部重排）。原因值得记下：

`src/main.js` 顶部把 `Math.random` 替换成种子化 PRNG，让布局每次加载都一致。而 three.js 里**每个 `Object3D` / `BufferGeometry` 构造时都会调用 `generateUUID()`，它消耗 4 次 `Math.random()`**。分块把 5 个 mesh 拆成几十个，多出的对象多抽了随机数（实测 `Math.random` 调用总数 1,567,118 → 1,567,310，正好 +192 = 48 个额外对象 × 4），于是这之后所有依赖随机的布局整体错位。

布局本身**仍然是确定性、可复现的**，只是与改动前不同。影响：

- README 里的截图（`preview.png`、`screenshots/*.png`）需要重拍。
- `placeSpecies` 里 `fixed` 数组手工放置的构图树仍在原位（它们在随机流之前），但周围的随机树变了，开场构图与之前不同。

**如果需要既要分块、又要保持布局不受未来重构影响**，根治方案是把种子随机数从 `Math.random` 里抽出来，改成场景生成代码显式调用的 `rand()` —— 这样对象创建就不再消耗布局用的随机序列。涉及 8 个文件，未做。

## 明确评估后决定不做的

**浮尘和飞鸟的视锥剔除**（"看不见的物体不要有动画"的 CPU 版本）。逐环节剖析每帧 CPU：

| 环节 | ms/帧 |
|---|---|
| composer.render | 1.8129 |
| particles（浮尘+飞鸟） | 0.0246 |
| updateWind | 0.0202 |
| controls | 0.0092 |
| move | 0.0083 |
| grass LOD | 0.0037 |
| stream | 0.0019 |

`composer.render()` 占 96%，**所有动画更新加起来 0.068ms**。给 160 个浮尘和 6 只鸟加视锥判断最多省 0.02ms（预算的 0.1%），而判断本身的开销就差不多这么多 —— 净负收益。这个需求在 GPU 侧的有效版本就是第 6 项分块（被剔除的格子连风的顶点着色器都不执行）。

**鹅卵石降面数**：420 颗最小仅 0.18 单位的石子每颗用了 320 个三角形，降到 80 面可省 10 万。但既然三角形不是瓶颈，收益为零，而它会额外扰动一次布局（新建几何体 = 4 次 `Math.random`）—— 不值得。

**60 FPS 上限**：本机是 60Hz 显示器，加了是死代码。若在 120Hz ProMotion 机器上运行，加一个帧率上限可直接再省一半，届时再加。

## 仍然可做的（按片元收益排序）

既然瓶颈在片元侧，后续应该往这个方向走：

- **水面着色器** —— 每片元约 9 次 value noise（涟漪 3 层、条纹 1 次、法线 3 次、闪光 2 次），且材质 `transparent: true` 无法享受 early-Z，屏幕上每一块水面都全额着色。合并采样（例如法线用解析导数替代 3 次采样、闪光两层合一）是当前最大的单点收益。
- **树叶重叠绘制** —— alpha 测试卡片在树冠内层层重叠，同一像素被着色多次。降低近处树的 `tiers`/`branches` 密度，或给远处树换更少卡片的 LOD，直接减少重叠层数。
- **草丛密度** —— 60,000 丛，已有分块 + 距离衰减（60m 内满密度，衰减至 15%）。收紧成 40m / 下限 8% 能减少片元和三角形，代价是中景草变稀。
- **地面网格 96.8k 三角形** —— 220×220 分段，三角形不是瓶颈所以收益低，不优先。

---

# 2026-08-05 — 地图扩展 9 倍 + 按需加载（第 1–3 步，分支 master）

目标：世界线性放大 3 倍（面积 9 倍），内容改为以摄像机为中心按需加载，近处密度与开场构图完全保持。本次完成前 3 步，**草/花/树尚未改为按格生成**（见下方遗留问题）。

## 改动

**新增 `src/rng.js`** —— 把原先写在 `main.js` 顶部的种子 PRNG 搬出来，并加 `withSeed(seed, fn)` 与 `cellSeed(cx, cz, layer)`。

这是流式加载的前提。原来整个布局依赖**生成顺序**：一个全局序列，各模块按固定顺序取数。改成按需加载后顺序取决于玩家怎么走，同一格子两次加载会长出不同的树。`withSeed` 让每格拥有由**坐标**派生的独立序列，位置决定内容，与访问时机和顺序无关。`cellSeed` 用 avalanche 混合而非简单异或——相邻格子的种子若相关，布局上会出现可见的对角条纹。

好处：现有一百多处裸 `Math.random()` 调用**一行都不用改**。

**`src/streamPath.js`**

1. `streamAt()` 改为两段搜索（每 8 个样本一个 node，先粗后精）。**这是整个方案的前提**：9 倍面积 × 3.2 倍曲线 = 27 倍地形采样，而这是全项目最热的函数。
   - 关键在于它是**精确**的，不是近似：粗筛给出真实最近距离的上界 B，任何距离 > B + SLACK 的 node 都不可能藏着更近的样本（SLACK = 半步长 × 实测最大样本间距）。
   - 我最初写的是「只取最近的那个 node」，那是近似的——两段河道靠近时最近的 node 和最近的样本可能属于不同河道。实测 0.13% 的查询与全扫描不一致。误差本身极小（最大 0.51，远小于代码里刻意加的 ±1.6 岸线抖动），**但它扰动了共享随机序列、把草的布局搅乱了**，于是失去了「指纹一致」这个验证后续改动的手段。保留上界判据多花约 2 倍粗筛开销，换来逐位精确。
   - 实测：70 万点网格零不一致；1.31 → 0.23 µs/查询（比原始短曲线的全扫描还快）。

2. 控制点两端各加 8 个（±150 → ±470），曲线长度 326.4 → 1051.8（3.22 倍）。Catmull-Rom 每段只由 4 个控制点决定，所以原河道内部（t 0.13–0.87）位置偏移 ≤0.12；只有最外两段会动，而它们本来就是最未经构图的部分，加水后变成普通中段河道。

3. **高程剖面从 t 空间改写成弧长空间**。这是「延长曲线但不动原河道」的关键：`t` 是总长的比例，任何按 t 定义的特征都会随曲线变长而滑动；按「距源头多少世界单位」定义则完全不动。
   - `ORIGIN_T` / `ORIGIN_SPAN_T` 是把原曲线投影到新曲线后的最小二乘拟合结果，不是算术推导——`getPointAt()` 按自身弧长表重参数化，手算偏移会差约 0.6 单位。
   - 新跌水沿用原有 ~39 单位间距，但必须与原河道留出 `REACH_CLEARANCE` 余量：不留时第一个上游跌水落在原河道末端**1 厘米**外，它的 smoothstep 斜坡渗回来，把原有水位抬了 0.74。
   - 新水潭放在跌水**中点**（原有水潭正是这样放的），且必须满足与原布局相同的 1.667σ 间距（实测原布局最紧处就是这个值）；宽度/衰减直接复用原有的 8 组数值。
   - 导出 `HOME_T` / `LOOK_T` / `originalT()`，取代 `main.js`、`trees.js`、`stream.js` 里写死的 0.36 / 0.58 / 英雄石 t 值。

   实测保持度（对比 master 在原河道上的取值）：`levelAt` 2.0e-13、`halfWidthAt` 2.1e-13、`cascadeAt` 1.2e-13、`narrownessAt` 1.9e-14。相机 y=11.5、目标 y=7.2，与 master 一个数不差。

**新增 `src/streaming.js`** —— 100m 格子调度器。每帧 6ms 构建预算（16.7ms 一帧还要渲染），按到相机距离近者优先；相机移动超过半格才重新评估；卸载调 `dispose()`。`lod(dist)` 是可选的按距离细分级别，距离变化时重建该格。

**`src/ground.js`** —— `createGround()` → `createGroundLayer()`，按格出 tile。
- 顶点亮度抖动改为**由坐标哈希**得出，不再取随机数：相邻 tile 共享边，同一位置若取到两个不同随机值，接缝两侧亮度不同，草地上会出现一条线。
- tile 之间重叠 0.06，仅相切会在移动镜头下露出裂缝。
- 分段数按**到相机**的距离递减（74 / 36 / 16）。我第一版按格子自身坐标算 ring，那是错的——离世界原点越远地面永久变粗，而非离相机越远才变粗。

**`src/toon.js`** —— 新增导出 `toonifyMaterials(mats)`。`toonify(scene)` 靠遍历场景收集材质，而流式加载的格子在启动时还不存在，**地面材质从来没被 cel-shade 过**，读起来比周围一切都更平更闷。两个入口共享一个 `done` 集合，避免同一材质被注入两次着色代码。

**`src/world.js`** —— 阴影相机**跟随摄像机**（±110，瞄向前方），天空穹顶 500 → 2000 且跟随相机。
- 这是一个**行为变化**：阴影贴图从「全程只渲染一次」变成「按需重渲」（相机移动超过 8 单位，或有新格子载入）。原来的做法只在「世界是固定尺寸且启动即全部存在」时成立。
- ±110 配 1024 贴图是每 texel 0.21 单位，**比原来的 ±180 更清晰**；代价是只有相机周边有阴影。
- 穹顶必须跟随相机：它要比 far plane 更远才能始终在背景，因此不能同时是世界里的固定物体，否则相机最终会穿出它的壳。

**`src/main.js`** —— far plane 1200 → 1600；接入 streaming；阴影按需刷新。

## 验证（自起 headless Chrome，走 CDP，未碰用户的浏览器）

新增 `.shot/` 下四个工具：`cdp.mjs`（截图 / 布局指纹 / renderer 统计）、`diff.mjs`（分区亮度对比）、`pixels.mjs`（射线追踪问「这个像素是什么」）、`walk.mjs`（驱动相机走全图并检查流式加载不变量）。

截图**无法逐字节对比**——水面 uTime、浮尘、风都随时钟前进，同一份代码两次运行的 PNG 就不同。所以用**布局指纹**（实例矩阵的 FNV-1a 哈希）：同一份代码两次运行指纹完全一致（已做对照），这才是「布局没变」的真正含义。

结果：

- **第 1 步逐像素级保持**：布局指纹与 master **完全一致**，console 干净。
- **第 2 步开场构图保持**：相机 y=11.5、目标 y=7.2 与 master 相同；截图确认湖面、英雄石、阶梯瀑布、岸边花丛都在原位。
- **第 3 步走全图**（10 个航点，含四角与两次回访）：
  - 地面空洞：**0**（每站 81 条向下射线全部命中）
  - 回访确定性：两次回访点的地形高度哈希**完全一致**
  - `geometries` 起始 139 / 峰值 144 / 结束 126 —— 有界，无单调增长，dispose 生效
  - console 干净
- 静止时实测：228 draw call、119 万三角形、156 个地面格。

## 遗留问题

- **草、花、树仍是启动时一次性生成，且仍限制在原 290×290 范围内**。所以走出中心区域会看到**有地面、有溪流、但没有草木**的区域。这是计划里的第 4 步，未做。
- `stream.js` 水面 ribbon 仍是整条一个 mesh（包围盒横跨全图，永远剔不掉），段数未按新长度加密；岩石/卵石数量未按曲线变长而增加。计划第 5 步，未做。
- `particles.js` 浮尘与飞鸟中心仍写死 `(0, -16)`，未跟随相机。计划第 5 步，未做。
- 扩展后的画面整体亮度比 master 低约 4.9（分区最大 −14 到 −23）。已排查并修掉其中一项真实缺陷（地面材质漏 toonify，右侧色差从 −29.4 收敛到 −0.6）。剩余差异主要来自**地图真的变大了**——对照实验（把地面半径临时缩回 150）显示中间区差异从 −17.7 收敛到 −3.6，即那些像素以前是天空、现在是远处草地与山坡。这是预期效果而非缺陷，但**尚未逐项确认全部残差**。
- `src/chunks.js` 的职责将被 `streaming.js` 接管，但目前 `trees.js` 仍在用它；`src/godrays.js` 无任何引用。按 CLAUDE.md §3 未删除，留待决定。

## 本次的一个流程错误（记下来避免重犯）

改完 `createTrees` 的签名后没有改调用处，并且在**那次编辑之后没有重新在浏览器里验证**，就对外说「可运行」。实际抛出：

```
Uncaught TypeError: Cannot destructure property 'radiusNear' of 'undefined'
    at createTrees (trees.js:50)
```

同一次还有第二个同类错误：`streaming.js` 里用了一个没声明的临时向量 `scanAt`，抛 `ReferenceError`。

两个都是在最后一次浏览器检查**之后**引入的，所以那句「已验证」引用的是过期的测量结果。教训很直接：**每次编辑之后、给出结论之前，重新跑一次 probe**；不能拿编辑前的验证结果为编辑后的代码作保。`.shot/cdp.mjs probe` 已经会把 console 里的异常单独打印出来（本次据此定位），代价只有约 40 秒。

`trees.js` 已回退到仅含 `HOME_T` 锚点这一处改动。修复后重跑：console 干净，走图测试 10 个航点地面空洞 0、两次回访完全一致、geometry 144→149→126 有界。

## 2026-08-05 · master · 修复地面闪烁（用户报告）

用户报告：「地面经常闪烁？有几个地面的格子消失了？然后又出现」。

### 定位

原有的 `.shot/walk.mjs` **在结构上不可能发现这个问题**：它每次传送后都等队列排空才测量，量到的永远是稳定态；而闪烁是运动过程中的瞬态。新增 `.shot/flicker.mjs`：让相机逐帧连续平移，**每帧**采样 ground 层已加载的格子集合，凡是「上一帧有、这一帧没有、但仍然远在半径之内」的格子就记为一次可见的空洞，并统计持续帧数。为此在 `streaming.js` 上加了一个诊断接口 `loadedKeys(id)`。

实测（四条腿，各 400 帧，约 360 单位行程）：

| | 一次性 disposeが原样 | 仅禁用 LOD 重建路径（对照） | 修复后 |
|---|---|---|---|
消失次数 | **372** | 0 | **0**
最长空洞 | **8 帧**（约 130ms） | 0 | 0
队列峰值 | 22–26 | 8–10 | 22–26

对照实验只改一个地方（把 LOD 变化那一支短路掉），372 → 0，因此原因是确定的，不是猜的。

### 原因

`rescan()` 发现某格的 LOD 档位该变了，就**立刻 dispose 并从 loaded 里删掉**，指望它在下面的循环里被重新入队。但重建要走和其他格子共用的、每帧 6ms 预算的队列，所以从删除到重建之间这个格子**真的什么都没有**。LOD 边界是固定的 250/450 环，相机移过半格时整整一环同时翻档，于是一批瓦片一起消失、一两帧后再一起出现。

### 改法

改成**先建后换**：LOD 变化时不再 dispose，而是入队一个 `replace: true` 的任务，旧瓦片留在场景里；等新瓦片建好，才 dispose 旧的并替换。两个边界情况也一并处理：替换任务轮到时格子已因离开半径被卸载 → 直接丢弃（否则会把相机已经离开的格子复活）；相机又走回来、想要的档位和现有的一样 → 也直接丢弃。

修复后队列峰值仍是 22–26（与出 bug 的版本相同，而不是对照组的 8–10），说明 LOD 重建照旧在做，只是不再暴露成空洞。

### 顺带修掉的第二个缺陷

`update()` 里算格子世界坐标时用的是硬编码的 `CELL`，而不是该层自己的 `layer.size`（`x0: job.i * CELL`、`cellCentre(job.i)`、`size: CELL`）。任何用粗网格注册的层——也就是计划里「树排到天边」的远景层要用的——都会被建到错误的世界位置上。目前两层都用默认尺寸，所以还没暴露。已改为一律用 `layer.size`。

### 验证

- `.shot/flicker.mjs`：四条腿全部 0 次消失（修复前 372 次）。
- `.shot/walk.mjs`：10 个航点地面空洞 **0**，两次回访**完全一致**，`geometries` 144 → 峰值 149 → 结束 128（有界，先建后换没有引入泄漏），console 干净。
- **开场构图零变化**：以修复前的 `streaming.js` 为对照各截一张图对比，9 个分区亮度差**全部为 0**，仅 top-centre 有 0.1% 像素不同（飞鸟与浮尘本身在动）。稳定态下这个改动不可见。

### 遗留问题

不变，仍是上一节列出的那些（草木未按格生成、stream 未分段、particles 未跟随）。本次没有动这些。

## 2026-08-05 · master · 第 4 步：树与花草改为按格生成

计划第 4 步的剩余部分（草地上次已完成）。

### 树（`trees.js`）：两层，物种互不重叠

拆成 `treesNear`（小叶榄仁、高松、银杏，半径 260、格 100）与 `treesFar`（松、云杉，半径 700、格 **350**）。**两层的物种集合完全不相交**，这是关键：

- 若按「近层画全部物种、远层画便宜的子集」来分，同一个格子会被两层各放一次云杉，近处树量翻倍；
- 若某层的物种集合随距离变化，穿过分界时树会成批出现/消失。

物种不相交，这两种情况都不可能发生。代价是榄仁/银杏在 260 处淡入，但它们本来就被自身的 `minD`/`maxD` 限制在离溪流 45–110 内，从来不是地平线要素；地平线是松与云杉，正由远层承担。

远层用 350 的粗网格：100 的话地平线那一环约 150 格、每格数个 InstancedMesh，等于为几像素高的树付出几百个 draw call。

撒点从「全图凑够 count」改为**按面积的密度**。密度是**实测**的，不是从 count 反推的——各物种接受率差别很大（银杏 15%、松 74%），所以按面积撒需要的是「每平方米尝试次数」。不足 1 次的小数部分按概率进位，否则 0.00094/m² 的高松在 100m 格里会被截断掉 4%。

### 花草（`foliage.js`）：曲线段要**划分**，不能重叠

四段里两段按面积（草地小花、绒球灌木），两段沿曲线（岸边花园、鲁冰花穗）。沿曲线的那两段是难点，做法是**把曲线按格划分**：每格只在「曲线点落在本格内」的那些弧段上撒簇，每段弧只属于一个格子。

一开始写成了「每格考虑自己附近（±34）的曲线，再用 `owns()` 过滤落在格内的成员」——**实测偏密**。一段曲线同时落在很多格子的「附近」，每格都按完整速率撒一遍，事后过滤成员并不能抵消。在同一段曲线上对比：鲁冰花穗 **3.5 倍**；而岸边花园恰好没超（成员只散布 ±1.6，过滤几乎正好抵消过撒）——这种巧合正是不该依赖的东西。改成划分后全部归位。

### 验证

**密度**（这一步的验收标准）：

| | 原版 | 按格 | 比值 |
|---|---|---|---|
5 个树种合计 | 353 | 359.6 | **1.019**
距开场相机 40 内的树 | 11.4 | 11.5 | 1.011
距开场相机 80 内 | 75.2 | 76.0 | 1.010
岸边花园（4 species 合计） | 143.8 | 144.4 | 1.004
鲁冰花穗 | 327.8 | 309.5 | 0.944
草地小花 | 1400 | 1397.5 | 0.998
绒球灌木 | 140 | 136.5 | 0.975

树的数字是 **40 次独立实现的平均**：单次跑出来是 1.079，看着像偏密，但 ~350 棵的泊松噪声本身就有 8–10%，单次无法区分偏差与噪声；平均后噪声按 1/√n 衰减而偏差不会，结果是 1.019，说明单次那个 1.079 是噪声。花草同理取 12 次平均，并且**限制在与原版相同的那段曲线上**比较——第一次对比时「原版」只在原始 326 单位上撒，而「按格」统计了 ±350 内所有格子（包含大量**延长后**的曲线），于是得出假的 2.4 倍。

**运行时**：

- 走图 10 个航点：地面空洞 0，两次回访完全一致，`geometries` 139 → 峰值 144 → 结束 128（有界），console 干净。
- 逐帧闪烁：四条腿全部 **0 次消失**（上一节的先建后换依然成立，加入三个新层后队列峰值 27–32 仍无空洞）。
- **树木布局确定性**：单独写了按实例矩阵世界坐标做 FNV-1a 的哈希（走图脚本只哈希地形高度，那是 `terrainHeight()` 的性质，就算每棵树都挪了也看不出来）。两次回访哈希完全一致。
  - 中途两次「不一致」都是**测量口径错误**，不是代码缺陷：哈希窗口取得比最小层半径还大，把「哪些格子恰好还驻留」也算进去了。卸载有意留了 `radius + size` 的滞后，所以从远处走回来时确实会比冷启动多驻留一些远格（回访时数量是升高的，3651 → 3786，正是这个特征）。窗口收到 180（小于所有会投影的层的半径，foliage 最小 220）后两次回访完全一致。
- 静止实测：283 draw call、235 万三角形、548 mesh、53238 个实例；格子数 ground 156 / grass 7 / treesNear 22 / treesFar 12 / foliage 16。draw call 从改前的 228 升到 283，仍在计划预估的 400 以内。

### 顺带发现（未改，按 CLAUDE.md §3 只报告）

四棵手工摆放的框景树（2 棵榄仁、2 棵银杏）**一直没有被放出来**：它们所在位置地形高 2.90，而水位 4.50，被 `inWater` 判定在水里而拒绝。这不是本次引入的——HEAD 里那段注释自己就写着「水潭后来变宽，把其中一些吞掉了」。是既有问题，留待决定。

### 遗留问题

- `stream.js` 水面 ribbon 仍是整条一个 mesh，段数未按新长度加密；岩石/卵石数量未随曲线变长增加。计划第 5 步。
- `particles.js` 浮尘与飞鸟中心仍写死 `(0, -16)`，未跟随相机。计划第 5 步。
- `src/chunks.js` 现在**已无任何引用**（trees 与 foliage 都已改用格子做剔除单位）；`src/godrays.js` 同样无引用。按 CLAUDE.md §3 均未删除，留待决定。

## 2026-08-05 · master · 第 5 步：粒子跟随；溪流分段**经实测放弃**

### 先说放弃的那一项：溪流 ribbon 分段

计划里第 5 步的主项是「把水面 ribbon 切成约 12 段以便视锥剔除」，理由是「整条一个 mesh 的包围盒横跨全图，永远剔不掉」。**实测不支持这个理由，所以没做。**

把相机转到背对山谷（在 420,26,-420 看向 700,-700），对比两条 ribbon 显示与隐藏：

| | draw call | 三角形 | 帧中位数 |
|---|---|---|---|
有 ribbon | 69 | 338536 | 0.2 ms
隐藏 ribbon | 68 | 337576 | 0.2 ms

代价是 **1 个 draw call、960 个三角形**（占 33.8 万的 0.3%），帧时间没有差别。两条 ribbon 的 `frustumCulled` 本来就是 true；包围球半径 472 且相机在球内，three.js 确实无法整体剔除，但真正提交到 GPU 的部分很少，屏幕外的几何被廉价丢弃了。切成 12 段换来的收益在测量精度以下，只会增加复杂度，因此不做，并把这个结论记下来，而不是照着计划做无用功。

（顺带修正测量方法：第一版计时脚本读到的是 `calls:1, tris:1` —— `renderer.info` 每次 `render()` 都会重置，而 rAF 帧最后是 composer 的全屏 quad。必须在直接 `render()` 之后**立刻**读计数器。`cdp.mjs` 里本来就写着这个坑，我又踩了一次。）

第 5 步其余各项在前几步里已经做掉了，本次核对确认：

- 段数密度：`RIBBON_SEGS = 1353`，每段 0.7774 单位，原版 420 段每段 0.7772 —— 已随曲线加密。
- 岩石/卵石数量：已经走 `perLength()`。
- 阴影跟随相机（`focusShadow`）、天空穹顶跟随（`followSky`）、远平面 1600：均已接入主循环。

### 真正未做的那一项：粒子跟随（已完成）

浮尘与飞鸟的中心原先写死在 `(0, -16)`，也就是最初那片空地。300m 的图里它一直在视野内；现在走几秒就飞出尘埃，阳光柱变空，而 160 颗尘粒和 6 只鸟还在身后某处继续算。

两者跟随方式**故意不同**：

- **尘埃盒连续跟随**，但尘粒保留各自的世界坐标继续按自己的速度飘，只有回绕边界在移动。所以飘出盒子的尘粒会在盒内新位置重生，而不是整团锁着相机平移 —— 那样看起来就像镜头上的脏点。
- **飞鸟圆心跳变，带滞后**。鸟的位置每帧都由圆心推出来，圆心若连续爬行会把六只鸟一起横向拖过天空。所以圆心保持不动，直到相机离开飞行圈足够远（`BIRD_REHOME_DIST = 52`，大于最大飞行半径 38），才整体搬到相机处。

### 验证

- 跑到三个相距数百单位的位置：尘埃到相机的平均距离 13.1 / 12.8 / 12.9，**没有一颗尘粒被落在 200 单位以外**；六只鸟在每个位置都是 26.6–37.4。跟随生效。
- 相机静止时逐颗尘粒的位移各不相同（0.55 / 0.55 / 0.61 / 0.62，另有一颗 32.7 是回绕重生），证明它们仍在各自飘动，没有随相机刚性平移。
- 开场画面**零变化**：9 个分区亮度差全部 ≤0.1（相机起点正好是原来的空地中心，所以本该如此）。
- 走图：地面空洞 0，回访一致，`geometries` 129 → 峰值 134 → 118 有界，console 干净。
- 逐帧闪烁：四条腿 0 次消失，退出码 0。
- 树木布局确定性：两次回访哈希完全一致。
- 静止实测：259 draw call、234.7 万三角形。

## 2026-08-05 · master · 第 6 步：端到端验证

针对最初的需求本身（地图扩到 9 倍、按需加载）做的整体验证，覆盖前面各个 harness 没测到的三件事。

### 1. 冷启动

队列在我第一个可测量的帧之后 47 ms / 6 帧内排空。

**这个数字要打折看**：等到 `window.__scene` 存在、CDP 能求值时，主循环已经排了几帧了，所以「首帧待建格子数」读到的是 0，是个**下界**，并不能证明冷启动很轻。能证明的是它没有卡在一个巨大的首帧上——真正的证据是下面那条 3000 帧的曲线（p95 9.2 ms）。

### 2. 3000 帧连续行走（约 2700 单位，蜿蜒路径，不是传送）

| 指标 | 结果 |
|---|---|
终点 | (-606, 513) |
`geometries` | min 123 / max 144 —— 有界，无单调增长 |
帧时间 | 中位数 **8.3 ms**、p95 **9.2 ms**、最差 33.7 ms |
超过 33 ms 的帧 | **3000 帧里 1 帧** |
构建队列峰值 | 37 |
结束时格子数 | ground 175 / grass 11 / treesNear 30 / treesFar 19 / foliage 20 |

持续移动、格子不断吞吐的情况下 6 ms 构建预算是守得住的：p95 9.2 ms 仍在 60fps 的 16.7 ms 之内，只有 1 帧掉出去。

### 3. 到达范围

把相机放到 (1300, 1300)——远超 9 倍地图约 900 的跨度：

- 25 条向下射线 **25/25** 命中地面；
- 该处已加载 **437** 个投影实例（树 + 灌木），即植被确实跟到了这里；
- 格子数 ground 156 / grass 4 / treesNear 24 / treesFar 13 / foliage 16 —— 与原点附近同量级，说明是按相机邻域加载，而不是把整张图铺开。

### 总结

最初的三个方向性选择都已落地：溪流曲线同步延长到 3.22 倍、树分近远两档排到天边、近场密度与原版一致（树 1.019、岸边花园 1.004、草地小花 0.998、绒球灌木 0.975）。开场构图逐区亮度与改动前一致。

### 遗留问题（未做，按 CLAUDE.md §3 只报告不动）

- **四棵手工框景树一直没被放出来**：所在位置地形高 2.90 < 水位 4.50，被 `inWater` 拒绝。既有问题，HEAD 的注释已自述「水潭变宽把它们吞了」。
- `src/chunks.js` 现已无任何引用；`src/godrays.js` 同样无引用。均未删除。
- 溪流 ribbon 分段经实测放弃，理由见上一节。
